/**
 * The Carpeta Ciudadana's result pages run to half a megabyte and the sede
 * regularly takes 50 to 70 seconds to answer one, past the client's default.
 */
export const gobexTimeoutMs = 180_000
