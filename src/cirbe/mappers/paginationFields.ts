/** The single-page pagination block every IAS form posts back. */
export const paginationFields: Readonly<Record<string, string>> = {
  'Paginacion.PaginaActual': '1',
  'Paginacion.NumeroPaginas': '1',
  'Paginacion.NumeroRegistros': '1',
}
