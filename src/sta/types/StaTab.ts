/** One tab of an STA page, selected through a `submitAjax.aa` TABSEL. */
export type StaTab = {
  readonly pageCode: string
  readonly screenId: string
  readonly tabber: string
  readonly tab: string
}
