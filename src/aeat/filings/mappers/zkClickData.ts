/** The `data_0` payload of a ZK `onClick` event, with the coordinates the browser sends. */
export const zkClickData: Readonly<Record<string, unknown>> = {
  pageX: 1,
  pageY: 1,
  which: 1,
  x: 1,
  y: 1,
}
