/** The ZK desktop id and widget uuids of one fresh `index.zul` page. */
export type ZkDesktop = {
  readonly desktopId: string
  readonly comboModelos: string
  readonly comboEjercicios: string
  readonly comboPeriodos: string
  readonly buttonBuscar: string
}
